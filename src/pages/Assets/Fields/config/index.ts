// Экспорт конфигурации синхронизации
export { 
  fieldsSyncConfig, 
  syncOptions, 
  statusPanelConfig 
} from './syncConfig';

// Экспорт конфигурации таблицы
export { 
  fieldsColumns, 
  tableConfig, 
  mobileColumns, 
  compactColumns,
  filterConfig,
  type Field
} from './tableConfig';

// Экспорт форматтеров
export * from './formaters';

// Экспорт конфигурации формы редактирования
export { fieldFormConfig, validation } from './editConfig'; 
