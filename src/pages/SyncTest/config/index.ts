// Экспорт конфигурации синхронизации
export { 
  equipmentSyncConfig, 
  syncOptions, 
  statusPanelConfig 
} from './syncConfig';

// Экспорт конфигурации таблицы
export { 
  equipmentColumns, 
  tableConfig, 
  mobileColumns, 
  compactColumns,
  filterConfig 
} from './tableConfig';